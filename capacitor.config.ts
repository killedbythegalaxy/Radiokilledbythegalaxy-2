import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'vite-react-typescript-starter',
  webDir: 'dist'
};

export default config;
